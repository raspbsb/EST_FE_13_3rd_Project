import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      default: "#FBFCFD",
    },
    primary: {
      main: "#0D6EFD",
      light: "#569AFE",
    },
    secondary: {
      main: "#EDEDED",
    },
    text: {
      primary: "#212121",
    },
  },
});

export default theme;
